name: domain_auth
description: "Modular project for domain_auth"
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: __SDK_CONSTRAINT__

dependencies:
  flutter:
    sdk: flutter
  library_common:
    path: ../../library/library_common
  domain_common:
    path: ../../domain/domain_common
  injectable: any
  get_it: any
  freezed_annotation: any

dev_dependencies:
  build_runner: any
  flutter_test:
    sdk: flutter
  freezed: any
  injectable_generator: any
  lint: any
