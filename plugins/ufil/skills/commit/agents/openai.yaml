interface:
  display_name: "Commit Flutter Changes"
  short_description: "Create a conventional scoped commit"
  default_prompt: "Use $ufil:commit to create a conventional commit for these changes."

policy:
  allow_implicit_invocation: false
